interface Props{} // definimos una interfaz props vacía

const ErrorPage = (props: Props)=>{
    return(
        <div>
            <h1>404: Page not found</h1>
        </div>
    )
};

export default ErrorPage;